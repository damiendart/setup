# {{ ansible_managed }}
#
# This file was written by Damien Dart, <damiendart@pobox.com>. This is
# free and unencumbered software released into the public domain. For
# more information, please refer to the accompanying "UNLICENCE file.


export EDITOR="vim"
export GEM_HOME="$HOME/.gem"
export NPM_CONFIG_PREFIX="$HOME/.npm"
export PATH="$PATH:$GEM_HOME/bin:$NPM_CONFIG_PREFIX/bin:$HOME/.local/bin"

git config --global core.excludesfile "$HOME/.gitignore"
cat > "$HOME/.gitignore" <<GITIGNORE
*.swp
node_modules
GITIGNORE

{% if administrator_email_address is defined and administrator_full_name is defined %}
  git config --global user.email "{{ administrator_email_address }}"
  git config --global user.name "{{ administrator_full_name }}"
{% endif %}

export VIRTUALENVWRAPPER_PYTHON="/usr/bin/python3"
source "$HOME/.local/bin/virtualenvwrapper_lazy.sh"
